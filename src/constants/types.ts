export interface Project {
  title: string;
  imageUrl: string;
  imageAlt: string;
  description: string;
  githubLink: string;
  liveSiteLink: string;
  tags: string[];
}

export interface Skill {
  title: string;
  level: number;
}

export interface Skill {
  title: string;
  level: number;
}

export interface Project {
  title: string;
  description: string;
  imageUrl: string;
  imageAlt: string;
  githubLink: string;
  liveSiteLink: string;
  tags: string[];
}
