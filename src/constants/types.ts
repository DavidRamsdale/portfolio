export interface Skill {
  title: string;
  level: number;
}

export interface Skill {
  title: string;
  level: number;
}

export interface Project {
  title: string;
  description: string;
  imageUrl: string;
  imageAlt: string;
  githubLink: string;
  liveSiteLink?: string;
  tags: string[];
}
